// src/components/Header.js

import React from "react";
import "./Header.css"; // Import the CSS file for styling

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">Welcome to Room ____</h1>
    </header>
  );
};

export default Header;
